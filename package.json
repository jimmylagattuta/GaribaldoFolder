{
  "name": "garibaldos-nursery",
  "private": true,
  "engines": {
    "node": "22.14.x"
  },
  "scripts": {
    "build": "npm --prefix client run build",
    "heroku-postbuild": "npm --prefix client ci && npm --prefix client run build"
  }
}